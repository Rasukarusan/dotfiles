[user]
	name = 
	email = 
[core]
	excludesfile = ~/.gitignore_global
	quotepath = false
	editor = nvim
	hooksPath = ~/dotfiles/terminal/git/hooks
	# pager = diff-so-fancy | less --tabs=4 -F -S -R -M -i
        # pager = delta --diff-so-fancy --line-numbers 
[color]
	ui = true
[color "diff-highlight"]
	oldNormal = red 
	oldHighlight = red  52
	newNormal = green
	newHighlight = green 22
[color "diff"]
	meta = yellow
	frag = magenta 
	commit = yellow 
	old = red 
	new = green 
	whitespace = red reverse
# [delta]
    # diff-so-fancy = true
    # line-numbers = true
    # features = side-by-side line-numbers decorations
    # syntax-theme = Dracula
